storage:
  postgres:
    postgres_db:
      username:
        env: SCHEDULER_DB_USERNAME
      password:
        env: SCHEDULER_DB_PASSWORD
      hostname:
        env: SCHEDULER_DB_HOSTNAME
      db_name:
        env: SCHEDULER_DB_NAME
      port: 5432
